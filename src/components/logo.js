// ŌMZIG overlay: the upstream base64 CIPP logo is replaced by the ŌMZIG
// wordmark component (see src/omzig/README.md). CyberDrain attribution and
// the AGPL-3.0 notice remain on the /license page.
import { OmzigLogo } from "../omzig/branding/OmzigLogo";

export const Logo = (props) => <OmzigLogo {...props} />;
